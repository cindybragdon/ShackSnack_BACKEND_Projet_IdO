export const regexPositiveInt = new RegExp(/^[0-9]\d*(\.\d+)?$/);
export const regexEmail = new RegExp(/^[\w\.-]+@[a-zA-Z\d\.-]+\.[a-zA-Z]{2,6}$/);
export const regexInt = new RegExp(/^-?\d+$/);
export const regexPositiveNumber = new RegExp(/^\d+(\.\d+)?$/);